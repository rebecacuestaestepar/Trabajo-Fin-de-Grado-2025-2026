\apendice{Documentación técnica de programación}

\section{Introducción}

En este apéndice se recoge la documentación técnica de programación del proyecto. Su objetivo es facilitar la comprensión de la estructura interna de la aplicación, así como proporcionar la información necesaria para que otro desarrollador pueda instalar, ejecutar, mantener o ampliar el sistema.

La aplicación desarrollada está formada por un \textit{backend} implementado con Django, un \textit{frontend} desarrollado con React y Vite, y una base de datos MySQL. El \textit{backend} se encarga de la lógica de negocio, la autenticación, la gestión de reservas, la carga de horarios y el acceso a la base de datos. El \textit{frontend}, por su parte, proporciona la interfaz de usuario desde la que se accede a las distintas funcionalidades del sistema.

Esta documentación describe la estructura de directorios del proyecto, las principales partes del código, los pasos necesarios para instalar y ejecutar la aplicación, y las pruebas realizadas para comprobar el correcto funcionamiento del sistema.

El proyecto se organiza separando el código del \textit{backend} y del \textit{frontend}, lo que permite mantener diferenciadas la lógica de negocio y la interfaz de usuario. Esta separación facilita el mantenimiento del sistema y permite que ambas partes puedan evolucionar de forma independiente.

\section{Estructura de directorios}

La estructura del proyecto se organiza separando la parte correspondiente al \textit{backend} y la parte correspondiente al \textit{frontend}. Esta división permite diferenciar claramente la lógica de negocio y acceso a datos, desarrollada con Django, de la interfaz de usuario, desarrollada con React. Por otro lado, el proyecto incluye un \textit{script SQL} de la base de datos llamado "BaseDeDatosHorario.sql", que se encuentra a la misma altura que el directorio del \textit{frontend} y del \textit{backend}.

En los siguientes apartados se describe la organización de ambos bloques, indicando la finalidad de los principales directorios y ficheros que forman parte del proyecto. Esta explicación facilita la comprensión del código fuente y permite que otros desarrolladores puedan localizar de forma más sencilla los módulos principales de la aplicación.


\subsection{Backend}

El \textit{backend} de la aplicación se ha desarrollado con Django y contiene la lógica principal del sistema. En esta parte del proyecto se gestionan los modelos de datos, las rutas, las vistas, la autenticación, la comunicación con la base de datos y las operaciones relacionadas con horarios, reservas, calendario académico, aulas, docencia y usuarios.

En la Figura~\ref{fig:estructura-directorios-backend} se muestra la estructura principal de directorios correspondiente al \textit{backend} del proyecto.


\begin{figure}[H]
	\centering
	\includegraphics[width=0.45\textwidth]{img/EstructuraDirectoriosBack.png}
	\caption{Estructura de directorios del backend}
	\label{fig:estructura-directorios-backend}
\end{figure}


Como se observa en la Figura~\ref{fig:estructura-directorios-backend}, el directorio del \textit{backend} está formado por diferentes aplicaciones de Django, cada una de ellas orientada a una parte funcional concreta del sistema. Esta organización modular permite separar responsabilidades y facilita el mantenimiento del código.

El directorio \textit{nucleo} contiene la configuración principal del proyecto Django. En él se encuentran los archivos generales de configuración, como la configuración del proyecto, las rutas principales y otros elementos necesarios para el arranque de la aplicación.

El módulo \textit{aulas} agrupa la lógica relacionada con la gestión de aulas y su ocupación. Este módulo permite trabajar con la información relativa a capacidad, ubicación y equipamiento disponible en cada aula, y hacer las llamadas para obtener las reservas asociadas a cada aula.

El módulo \textit{calendario} contiene las funcionalidades relacionadas con el calendario académico. En él se gestiona la información de días lectivos, festivos, exámenes, cambios de docencia y fechas asociadas a TFG/TFM. Se encarga de cargar el calendario académico obteniendo los tipos de día y de manejar el cambio de tipo de día, además de la generación de un nuevo calendario académico.

El módulo \textit{docencia} agrupa la información académica relacionada con grados, asignaturas, grupos y docentes. Esta parte resulta fundamental para la generación y consulta de horarios, ya que permite asociar las reservas periódicas con la estructura docente correspondiente. En este módulo, se obtienen los cursos con horario cargado, los grados con docencia y sus semestres, las reservas periódicas asociadas a todo lo anterior, en cuanto a mostrar el horario. En cuanto a las reservas periódicas, maneja la creación de una nueva reserva periódica, la edición, su eliminación, las restricciones, la búsqueda de aulas disponibles frente a otras reservas periódicas.

El módulo \textit{app\_horarios} contiene las funcionalidades de importación y exportación de la base de datos conjunta o por entidades.

El módulo \textit{reservas} contiene la lógica relacionada con la gestión de reservas puntuales. En él se implementan las operaciones de creación, modificación, consulta, eliminación, y la carga de un horario creando con el analizador léxico y creando todas las reservas periódicas asociadas a la hoja de cálculo cargada.

El módulo \textit{usuarios} se encarga de las funcionalidades relacionadas con la autenticación, los usuarios y los roles. Esta parte permite controlar el acceso a las distintas funcionalidades de la aplicación en función del perfil asociado a cada usuario. Por lo que en esta parte se encuentra la conexión con la API del moodle de la universidad.

Además de estos directorios, aparecen varios ficheros relevantes en la raíz del proyecto. El archivo \textit{manage.py} es el punto de entrada para ejecutar comandos propios de Django, como iniciar el servidor de desarrollo, aplicar migraciones o crear usuarios administradores. El fichero \textit{requirements.txt} contiene las dependencias de Python necesarias para ejecutar el \textit{backend}. Por otro lado, los archivos \textit{package.json} y \textit{package-lock.json} recogen las dependencias asociadas a Node.js, utilizadas por la parte de \textit{frontend}. Finalmente, el directorio \textit{node\_modules} se genera automáticamente al instalar dichas dependencias.

A continuación, se adjunta la Tabla~\ref{tab:estructura-backend} a modo resumen de las funcionalidades que abarca cada módulo.

\begin{table}[H]
	\centering
	\begin{tabular}{l p{9cm}}
		\toprule
		\textbf{Directorio/Fichero} & \textbf{Descripción} \\
		\midrule
		\textit{nucleo} & Configuración principal del proyecto Django. \\
		\textit{aulas} & Gestión de aulas, capacidad, ubicación y recursos disponibles. \\
		\textit{calendario} & Gestión del calendario académico, días lectivos, festivos, exámenes y cambios de docencia. \\
		\textit{docencia} & Gestión de grados, asignaturas, grupos y docentes y horarios. \\
		\textit{app\_horarios} & Importación y exportación base de datos. \\
		\textit{reservas} & Gestión de reservas puntuales y analizador léxico. \\
		\textit{usuarios} & Gestión de autenticación, usuarios, roles y permisos. \\
		\textit{manage.py} & Archivo principal para ejecutar comandos de administración de Django. \\
		\textit{requirements.txt} & Fichero con las dependencias Python necesarias para el backend. \\
		\textit{package.json} & Fichero de configuración de dependencias de Node.js. \\
		\textit{node\_modules} & Directorio generado automáticamente al instalar dependencias de Node.js. \\
		\bottomrule
	\end{tabular}
	\caption{Descripción de la estructura principal del backend}
	\label{tab:estructura-backend}
\end{table}

\subsection{Frontend}

El \textit{frontend} de la aplicación se encuentra desarrollado con React + Vite y contiene la parte visual del sistema. Esta parte del proyecto se encarga de mostrar las pantallas de la aplicación, gestionar la interacción con el usuario y comunicarse con el \textit{backend} mediante peticiones HTTP.

En la Figura~\ref{fig:estructura-directorios-frontend} se muestra la estructura principal de directorios correspondiente al \textit{frontend} del proyecto.

\begin{figure}[H]
	\centering
	\includegraphics[width=0.45\textwidth]{img/EstructuraDirectoriosFront.png}
	\caption{Estructura de directorios del frontend}
	\label{fig:estructura-directorios-frontend}
\end{figure}


Como se observa en la Figura~\ref{fig:estructura-directorios-frontend}, el directorio del \textit{frontend} está formado por una carpeta principal \textit{src}, donde se encuentra el código fuente de la interfaz, y por varios ficheros de configuración necesarios para ejecutar y construir la aplicación.

El directorio \textit{public} contiene recursos estáticos públicos que pueden ser utilizados directamente por la aplicación. Estos archivos se sirven sin necesidad de ser procesados por el sistema de construcción.

Dentro de \textit{src} se organiza la mayor parte del código del \textit{frontend}.

El directorio \textit{administrador} agrupa las pantallas y componentes relacionados con el panel de administración. En este módulo se encuentran las funcionalidades destinadas a la gestión de entidades, usuarios, roles y el mantenimiento global del sistema.

El directorio \textit{api} contiene las funciones encargadas de centralizar la comunicación con el \textit{backend}. Esta organización permite separar las llamadas HTTP de los componentes visuales, facilitando el mantenimiento del código y evitando duplicidades.

El directorio \textit{assets} almacena recursos estáticos utilizados por la interfaz, como imágenes, logotipos u otros elementos gráficos.

El directorio \textit{aulas} agrupa los componentes y páginas relacionados con la consulta de ocupación de aulas.

El directorio \textit{auth} contiene la lógica para proteger rutas de acceso sin autenticación, y para validar permisos.

El directorio \textit{calendario} agrupa las pantallas y componentes relacionados con la consulta y modificación del calendario académico.

El directorio \textit{horarios} contiene las pantallas y componentes necesarios para cargar nuevos horarios, consultar horarios existentes.

El directorio \textit{login} contiene la pantalla de inicio de sesión y los elementos visuales relacionados con el acceso a la aplicación.

El directorio \textit{reservas} agrupa las pantallas y componentes relacionados con la gestión de reservas puntuales y periódicas, incluyendo listados, formularios de creación y edición, y acciones asociadas a las solicitudes.

Finalmente, el directorio \textit{shared} contiene elementos compartidos por diferentes módulos del \textit{frontend} como la modal de confirmación para eliminar, permitiendo reutilizar código común y mantener una estructura más organizada.

Además de estos directorios, aparecen varios ficheros relevantes en la raíz del proyecto. El archivo \textit{App.jsx} define la estructura principal de la aplicación React y la organización general de sus rutas o componentes principales. El archivo \textit{main.jsx} actúa como punto de entrada de la aplicación. Los ficheros \textit{App.css} e \textit{index.css} contienen estilos globales utilizados por la interfaz.

El fichero \textit{package.json} recoge la configuración del proyecto, los scripts de ejecución y las dependencias necesarias. El archivo \textit{package-lock.json} almacena la versión exacta de las dependencias instaladas. El archivo \textit{vite.config.js} contiene la configuración de Vite, herramienta utilizada para ejecutar y construir el \textit{frontend}. Por último, el directorio \textit{node\_modules} se genera automáticamente al instalar las dependencias de Node.js y no forma parte del código fuente desarrollado.

Se ha realizado una Tabla~\ref{tab:estructura-frontend} a modo resumen visual de lo que realiza cada directorio.

\begin{table}[H]
	\centering
	\begin{tabular}{l p{9cm}}
		\toprule
		\textbf{Directorio/Fichero} & \textbf{Descripción} \\
		\midrule
		\textit{public} & Recursos estáticos públicos de la aplicación. \\
		\textit{src} & Directorio principal del código fuente del frontend. \\
		\textit{administrador} & Pantallas y componentes relacionados con el panel de administración. \\
		\textit{api} & Funciones encargadas de realizar peticiones al backend. \\
		\textit{assets} & Imágenes, logotipos y otros recursos gráficos. \\
		\textit{aulas} & Componentes relacionados con aulas y ocupación de espacios. \\
		\textit{auth} & Lógica de autenticación y gestión de sesión. \\
		\textit{calendario} & Componentes relacionados con el calendario académico. \\
		\textit{general} & Componentes comunes de la interfaz. \\
		\textit{horarios} & Pantallas de carga, consulta y gestión de horarios. \\
		\textit{login} & Pantalla y componentes de inicio de sesión. \\
		\textit{reservas} & Gestión de reservas puntuales. \\
		\textit{shared} & Componentes o utilidades compartidas por varios módulos. \\
		\textit{App.jsx} & Componente principal de la aplicación React. \\
		\textit{main.jsx} & Punto de entrada del frontend. \\
		\textit{vite.config.js} & Configuración de Vite. \\
		\textit{package.json} & Dependencias y scripts del proyecto frontend. \\
		\textit{node\_modules} & Dependencias instaladas automáticamente mediante Node.js. \\
		\bottomrule
	\end{tabular}
	\caption{Descripción de la estructura principal del frontend}
	\label{tab:estructura-frontend}
\end{table}


\section{Manual del programador}

Esta sección está dirigida a futuros desarrolladores que necesiten mantener, ampliar o modificar la aplicación. En ella se describen los aspectos más relevantes del desarrollo, la organización interna del código y las partes principales que deben conocerse antes de realizar cambios en el sistema.

La aplicación está formada por dos bloques principales: el \textit{backend}, desarrollado con Django, y el \textit{frontend}, desarrollado con React. El \textit{backend} se encarga de la lógica de negocio, la autenticación, la comunicación con la base de datos y la validación de las operaciones. El \textit{frontend}, por su parte, proporciona la interfaz visual y se comunica con el servidor mediante peticiones HTTP.

\subsection{Organización interna del backend}

Como se ha indicado en la sección anterior, el \textit{backend} se encuentra dividido en varios módulos funcionales, como \textit{aulas}, \textit{calendario}, \textit{docencia}, \textit{reservas} y \textit{usuarios}. Cada uno de estos módulos agrupa la lógica correspondiente a una parte concreta del sistema, lo que permite separar responsabilidades y facilita el mantenimiento del código.

Dentro de estos módulos se ha seguido una organización basada en distintos tipos de ficheros, entre los que destacan los modelos, vistas, serializadores y servicios.

\subsubsection{Modelos}

Los modelos representan las entidades de la base de datos dentro de Django. Cada modelo define los campos de una tabla, sus tipos de datos, relaciones y restricciones principales. Gracias a estos modelos, el \textit{backend} puede trabajar con la información persistente de la aplicación sin necesidad de escribir directamente todas las consultas SQL.

En este proyecto, los modelos representan elementos como aulas, docentes, asignaturas, grupos, reservas, cursos, semestres y días del calendario académico. Cualquier modificación en la estructura de datos debe revisarse cuidadosamente, ya que puede afectar a las vistas, serializadores, servicios y pantallas del \textit{frontend} que dependan de dicha información.

\subsubsection{Vistas}

Las vistas son las encargadas de recibir las peticiones procedentes del \textit{frontend}, procesarlas y devolver una respuesta. En ellas se definen los puntos de entrada de la API y se controla qué operación debe ejecutarse en función de la petición recibida.

El \textit{frontend} envía una petición HTTP al \textit{backend}. La vista correspondiente recibe esa petición, comprueba los datos recibidos y coordina el resto de operaciones necesarias.

Aunque las vistas pueden contener validaciones sencillas, se ha procurado que la lógica más compleja se separe en servicios auxiliares. De esta forma, las vistas se mantienen más claras y se evita concentrar demasiada lógica en un único fichero.

\subsubsection{Serializadores}

Los serializadores se utilizan para transformar los datos entre el formato interno de Django y el formato intercambiado con el \textit{frontend}, normalmente JSON. Su función es especialmente importante en una aplicación con \textit{frontend} y \textit{backend} desacoplados, ya que permiten controlar qué información se envía al cliente y cómo se reciben los datos introducidos por el usuario.

Además de convertir objetos en respuestas JSON, los serializadores permiten realizar validaciones sobre los datos recibidos antes de crear o actualizar registros en la base de datos. Por ejemplo, pueden comprobar que los campos obligatorios estén presentes, que los tipos de datos sean correctos o que una estructura enviada desde el \textit{frontend} cumpla el formato esperado.

\subsubsection{Servicios}

Los servicios agrupan funciones auxiliares o procesos de negocio que no deben estar directamente dentro de las vistas. Esta separación permite reutilizar código y mantener una estructura más organizada.

En este proyecto, los servicios son especialmente importantes en procesos como la carga de horarios desde hojas de cálculo, la validación de disponibilidad de aulas, la comprobación de solapes entre reservas y el tratamiento del calendario académico.

Por ejemplo, en lugar de implementar toda la lógica de lectura de Excel dentro de una vista, esta se puede delegar en funciones o clases de servicio encargadas de procesar el fichero, extraer la información relevante y devolver los datos estructurados para que posteriormente sean validados y almacenados.

\subsubsection{Rutas}

Las rutas definen las direcciones de la API disponibles para el \textit{frontend}. Cada ruta se asocia con una vista o conjunto de vistas que implementan una funcionalidad concreta. Mantener las rutas organizadas por módulos facilita localizar los puntos de entrada de cada parte del sistema.

Si en el futuro se añade una nueva funcionalidad, será necesario definir su ruta correspondiente, implementar la vista asociada, crear o reutilizar los serializadores necesarios y, en caso de que exista lógica compleja, trasladarla a un servicio específico.

\subsection{Organización interna del frontend}

El \textit{frontend} está desarrollado con React y se organiza en módulos funcionales relacionados con las principales partes de la aplicación, como reservas, horarios, calendario, aulas, autenticación y administración.

Cada módulo contiene componentes y pantallas relacionados con una funcionalidad concreta. Esta organización permite que el código sea más mantenible y que los cambios realizados en una parte de la interfaz no afecten directamente al resto del sistema.

\subsubsection{Componentes}

Los componentes son la base de la interfaz en React. Cada componente representa una parte visual o funcional de la aplicación, como formularios, tablas, botones, modales, menús o pantallas completas.

En este proyecto se han utilizado componentes para construir pantallas como la gestión de reservas puntuales, la ocupación de aulas, el calendario académico, la carga de horarios o el panel de administración. La reutilización de componentes permite evitar duplicidades y mantener una apariencia coherente en toda la aplicación.

\subsubsection{Estado de los componentes}

Para gestionar la información interna de los componentes se ha utilizado el \textit{hook} \textit{useState}. Este mecanismo permite almacenar valores que pueden cambiar durante la ejecución de la aplicación, como filtros seleccionados, datos de formularios, mensajes de error, resultados de una consulta o el estado de apertura de un modal.

\subsubsection{Efectos y carga de datos}

Para ejecutar acciones asociadas al ciclo de vida de los componentes se ha utilizado el \textit{hook} \textit{useEffect}. Este mecanismo permite realizar operaciones cuando se carga una pantalla o cuando cambia algún dato del que depende el componente.

En este proyecto, \textit{useEffect} se utiliza principalmente para cargar información inicial desde el \textit{backend}. También puede emplearse para actualizar la información mostrada cuando el usuario modifica filtros o selecciona una nueva opción.

De esta forma, las pantallas pueden solicitar datos al \textit{backend} en el momento adecuado y actualizar la interfaz cuando la respuesta ha sido recibida.

\subsubsection{Comunicación con el backend}

La comunicación entre el \textit{frontend} y el \textit{backend} se realiza mediante peticiones HTTP. Para evitar duplicar código, las llamadas a la API se centralizan en ficheros específicos dentro del directorio \textit{api}.

Estos ficheros contienen funciones encargadas de realizar operaciones concretas, como consultar reservas, crear una reserva puntual, modificar una reserva periódica, cargar un horario o actualizar un día del calendario académico. Los componentes de React llaman a estas funciones cuando necesitan comunicarse con el servidor.

Esta organización permite separar la lógica visual de la lógica de comunicación. Si en el futuro cambia una ruta del \textit{backend}, solo será necesario actualizar el fichero correspondiente de la API, sin modificar todas las pantallas que utilizan esa operación.

\subsubsection{Gestión de formularios y eventos}

Las pantallas que permiten crear o editar información utilizan formularios controlados mediante estado. Cada campo del formulario se asocia a una variable de estado y se actualiza mediante eventos, como \textit{onChange} o \textit{onSubmit}.

Cuando el usuario confirma una operación, el componente recoge los valores almacenados en el estado, realiza las comprobaciones necesarias en la interfaz y envía los datos al \textit{backend}. Posteriormente, en función de la respuesta recibida, se muestra un mensaje de confirmación o de error.

\subsection{Modificación y mantenimiento del analizador de horarios}

Una de las partes más importantes del proyecto es el analizador encargado de procesar los horarios proporcionados en formato Excel. Este componente permite interpretar la información contenida en la hoja de cálculo, identificar asignaturas, grupos, aulas, días y franjas horarias, y transformar dichos datos en reservas periódicas dentro de la aplicación.

Debido a la importancia de este proceso, cualquier modificación sobre el analizador debe realizarse con especial cuidado. Antes de aplicar cambios directamente sobre el código integrado en la aplicación, se recomienda realizar las pruebas iniciales en el \textit{notebook} utilizado durante el desarrollo. Este \textit{notebook} ha servido como entorno de prueba para validar el funcionamiento del parser, comprobar la clasificación de celdas, revisar las expresiones regulares y verificar que la información extraída del Excel se interpreta correctamente.

El uso del \textit{notebook} permite probar cambios de forma más rápida y controlada, sin afectar directamente al funcionamiento del sistema completo. Por ello, si se necesita modificar alguna regla de lectura, clasificación o normalización, es conveniente comprobar primero el resultado en dicho entorno y, una vez validado, trasladar los cambios al código definitivo de la aplicación.

Para realizar pruebas sobre la carga de horarios se recomienda utilizar el fichero Excel situado en la carpeta de prototipos del proyecto. Este fichero se encuentra normalizado y adaptado al formato esperado por el analizador, por lo que permite comprobar el comportamiento del parser en un escenario controlado. En caso de que sea necesario modificar este Excel o crear uno nuevo, se deben revisar previamente las reglas definidas en la penúltima hoja del libro, donde se recogen las pautas utilizadas para estructurar la información.

Estas reglas son importantes porque el analizador depende de una serie de patrones y estructuras para identificar correctamente los elementos del horario. Entre ellos se encuentran los nombre abreviados de las asignaturas, los códigos de estas, los grupos tanto teóricos como prácticos y las aulas. Si el formato del Excel cambia sin respetar las reglas, es posible que el parser no clasifique correctamente alguna celdas o que de error, por ejemplo, si se cambia la abreviatura de la asignatura y no se encuentra unificada con la hoja de Asignaturas de la que se extrae el diccionario de código-asignaturas. De esta forma no se podrían generar las reservas periódicas esperadas.

Además debe prestarse especial atención a la normalización de los nombres de las aulas entre el Excel y la base de datos. El sistema incluye funciones específicas para adaptar los nombre leídos desde el Excel al formato almacenado en la base de datos. Transformando abreviaturas para que coincidan con los nombres registrados en la aplicación. Esta función transforma los prefijos <<Lab.>> y <<Lab>> en <<Laboratorio>> sino encuentra la estructura que siguen el nombre de las aulas, como por ejemplo <<Lab 42-A1>>, en ese caso lo transforma en <<Lab>> que es como se encuentran almacenadas en base de datos. Además elimina la palabra <<Aula>> salvo si es el aula de empresa o el <<Aula 0>>, elimina todo lo que viene entre paréntesis con ellos incluidos, y por último, cambia la abreviatura <<Inf.>> a <<Informática>>. De esta forma el resto del nombre que no modifica esta función, deberá de ser tal cual está en base de datos. Sino no encontrará el aula y no generará la reserva periódica aunque el analizador la haya extraído. Para ello consultar la nomenclatura de los aulas que tienen ahora mismo en el Excel unificado y no modificarla.

También es recomendable ejecutar las pruebas unitarias asociadas al analizador después de realizar cualquier cambio. Estas pruebas comprueban la clasificación de celdas mediante expresiones regulares, la normalización de nombres de aulas y la generación de reservas periódicas a partir de clases leídas del Excel. De esta forma, se puede detectar rápidamente si una modificación ha provocado un comportamiento incorrecto.

\subsection{Control de autenticación y permisos}

El sistema diferencia las funcionalidades disponibles en función del rol asociado al usuario autenticado. Por este motivo, el programador debe tener en cuenta que algunas pantallas, botones o acciones solo deben mostrarse a determinados perfiles.

En el \textit{frontend}, esta restricción se utiliza para adaptar la interfaz al rol del usuario, ocultando opciones que no le corresponden. Sin embargo, estas comprobaciones no son suficientes por sí solas, ya que un usuario podría intentar acceder manualmente a una ruta o enviar una petición no permitida.

Por este motivo, las validaciones importantes deben realizarse también en el \textit{backend}, que es el encargado de garantizar que cada operación solo pueda ser ejecutada por usuarios autorizados.

\section{Compilación, instalación y ejecución del proyecto}

En esta sección se describen los pasos necesarios para instalar y ejecutar el proyecto en un entorno local de desarrollo. La aplicación está formada por dos partes principales: el \textit{backend}, desarrollado con Django, y el \textit{frontend}, desarrollado con React y Vite. Además, es necesario disponer de una base de datos MySQL correctamente configurada.

El proceso de instalación consiste en preparar el entorno del \textit{backend}, instalar las dependencias necesarias, configurar la conexión con la base de datos, preparar el entorno del \textit{frontend} y ejecutar ambos servidores de desarrollo.

\subsection{Requisitos previos}

Antes de ejecutar el proyecto, es necesario tener instaladas las siguientes herramientas:

\begin{itemize}
	\item \textbf{Python}: necesario para ejecutar el proyecto Django.
	\item \textbf{pip}: gestor de paquetes de Python.
	\item \textbf{Node.js}: necesario para ejecutar el proyecto React.
	\item \textbf{npm}: gestor de paquetes de Node.js.
	\item \textbf{MySQL}: sistema gestor de base de datos utilizado por la aplicación.
	\item \textbf{Git}: recomendado para clonar el repositorio y gestionar el código fuente.
\end{itemize}

Además, se recomienda utilizar un entorno virtual de Python para aislar las dependencias del proyecto y evitar conflictos con otras aplicaciones instaladas en el equipo.

\subsection{Obtención del código fuente}

En primer lugar, se debe obtener el código fuente del proyecto desde el repositorio correspondiente. Para ello, se puede clonar el repositorio mediante Git:

\begin{verbatim}
	git clone <URL_DEL_REPOSITORIO>
\end{verbatim}

Una vez clonado el repositorio, se accede al directorio principal del proyecto:

\begin{verbatim}
	cd Trabajo-Fin-de-Grado-2025-2026
\end{verbatim}

En caso de disponer del proyecto comprimido en un fichero, bastará con descomprimirlo y acceder a la carpeta raíz del proyecto.

\subsection{Configuración de la base de datos}

La aplicación utiliza MySQL como sistema gestor de base de datos. Antes de ejecutar el \textit{backend}, es necesario crear una base de datos vacía y configurar las credenciales de acceso en el proyecto Django.

Desde MySQL, se puede crear la base de datos con un comando similar al siguiente:

\begin{verbatim}
	CREATE DATABASE nombre_base_datos;
\end{verbatim}

A continuación, se debe comprobar que el usuario configurado dispone de permisos suficientes sobre dicha base de datos. Estos datos de conexión deberán coincidir con los valores definidos en la configuración del proyecto.

En caso de partir de un script SQL inicial, este deberá ejecutarse sobre la base de datos creada para generar las tablas necesarias o cargar datos iniciales.

\begin{verbatim}
	mysql -u usuario -p nombre_base_datos < BaseDeDatosHorario.sql
\end{verbatim}

\subsection{Instalación y ejecución del backend}

Para instalar el \textit{backend}, se accede al directorio en el que se encuentra el proyecto Django:

\begin{verbatim}
	cd app_horarios
\end{verbatim}

A continuación, se crea un entorno virtual de Python:

\begin{verbatim}
	python -m venv .venv
\end{verbatim}

En Windows, el entorno virtual se activa con el siguiente comando:

\begin{verbatim}
	.venv\Scripts\activate
\end{verbatim}

Una vez activado el entorno virtual, se instalan las dependencias necesarias a partir del fichero \textit{requirements.txt}:

\begin{verbatim}
	pip install -r requirements.txt
\end{verbatim}

Antes de iniciar el servidor, se debe revisar la configuración del proyecto, especialmente los datos de conexión a la base de datos, la clave secreta de Django, el modo de depuración y los parámetros relacionados con la autenticación institucional.

Como el proyecto utiliza migraciones de Django para la gestión de usuarios, roles y permisos, se aplican mediante los siguientes comandos:

\begin{verbatim}
	python manage.py mmakemigrations
\end{verbatim}

\begin{verbatim}
	python manage.py migrate
\end{verbatim}

Finalmente, se inicia el servidor de desarrollo del \textit{backend}:

\begin{verbatim}
	python manage.py runserver
\end{verbatim}

Si la ejecución se realiza correctamente, el servidor quedará disponible por defecto en la siguiente dirección:

\begin{verbatim}
	http://127.0.0.1:8000/
\end{verbatim}

Una se ha inicializado el proyecto la primera vez correctamente, si el sistema operativo es Windows, puede ejecutar el archivo \textit{runserver.ps1} que inicia el servidor desde la carpeta principal del repositorio, al ejecutar los comandos necesarios.

Para instalar el \textit{frontend}, se accede al directorio correspondiente a la aplicación React:

\begin{verbatim}
	cd frontend_app_horarios
\end{verbatim}

A continuación, se instalan las dependencias definidas en el fichero \textit{package.json}:

\begin{verbatim}
	npm install
\end{verbatim}

Una vez instaladas las dependencias, se puede iniciar el servidor de desarrollo de Vite:

\begin{verbatim}
	npm run dev
\end{verbatim}

Si la ejecución se realiza correctamente, la aplicación indicará en la consola la dirección local en la que se encuentra disponible. Habitualmente, será una dirección similar a la siguiente:

\begin{verbatim}
	http://localhost:5173/
\end{verbatim}

Desde esta dirección se podrá acceder a la interfaz web de la aplicación mediante un navegador.

\subsection{Ejecución completa del sistema}

Para que la aplicación funcione correctamente en local, deben estar activos al mismo tiempo la base de datos, el servidor del \textit{backend} y el servidor del \textit{frontend}.

El orden recomendado de ejecución es el siguiente:

\begin{enumerate}
	\item Iniciar el servicio de MySQL.
	\item Comprobar que la base de datos existe y que las credenciales de conexión son correctas.
	\item Activar el entorno virtual del \textit{backend}.
	\item Ejecutar el servidor de Django mediante \textit{python manage.py runserver}.
	\item Instalar, si no se ha hecho previamente, las dependencias del \textit{frontend} mediante \textit{npm install}.
	\item Ejecutar el servidor de desarrollo del \textit{frontend} mediante \textit{npm run dev}.
	\item Acceder a la aplicación desde el navegador mediante la URL indicada por Vite.
\end{enumerate}

Durante la ejecución, el \textit{frontend} enviará peticiones HTTP al \textit{backend}, y este se encargará de procesarlas, aplicar la lógica de negocio y consultar o modificar la base de datos cuando sea necesario.

\subsection{Compilación del frontend para producción}

En caso de querer generar una versión optimizada del \textit{frontend} para despliegue, se puede ejecutar el siguiente comando dentro del directorio del \textit{frontend}:

\begin{verbatim}
	npm run build
\end{verbatim}

Este comando genera una versión compilada de la aplicación en el directorio de salida configurado por Vite, normalmente denominado \textit{dist}. Esta versión contiene los ficheros estáticos optimizados para su despliegue en un servidor.

\subsection{Consideraciones sobre variables de entorno}

Para evitar incluir información sensible directamente en el código fuente, se recomienda utilizar variables de entorno para almacenar datos de configuración. Entre los valores que pueden configurarse mediante variables de entorno se encuentran:

\begin{itemize}
	\item Clave secreta de Django.
	\item Nombre de la base de datos.
	\item Usuario y contraseña de la base de datos.
	\item Host y puerto de MySQL.
\end{itemize}

El uso de variables de entorno facilita el despliegue en distintos entornos, como desarrollo local, pruebas o producción, sin necesidad de modificar directamente el código fuente.

\section{Pruebas del sistema}

En esta sección se recogen las pruebas realizadas para comprobar el correcto funcionamiento de algunas de las funcionalidades principales de la aplicación. El objetivo de estas pruebas es verificar que el sistema responde de forma adecuada ante distintos escenarios, tanto correctos como erróneos, y que la lógica interna implementada en el \textit{backend} mantiene la coherencia de los datos.

Durante el desarrollo del proyecto se han implementado pruebas unitarias. Las pruebas unitarias son aquellas que permiten comprobar el funcionamiento de una parte concreta del sistema de forma aislada, normalmente una función, método o servicio específico. Este tipo de pruebas resultan útiles para validar que una unidad de código produce el resultado esperado ante unos datos de entrada determinados.

Las pruebas se han desarrollado utilizando la clase \textit{TestCase} de Django. Esta clase permite crear una base de datos de prueba independiente, de forma que los datos utilizados durante la ejecución de los tests no afectan a la base de datos real de la aplicación. Además, en algunos casos se han utilizado \textit{mocks} mediante \textit{unittest.mock.patch}, lo que permite simular el comportamiento de determinadas funciones auxiliares y comprobar cómo responde el servicio principal ante distintos resultados.

\subsection{Casos de prueba unitarios}

En este apartado se van a presentar algunos de los casos de prueba implementados. Para cada caso de prueba se ha definido un identificador, título, prioridad, descripción, precondiciones, pasos de ejecución, resultado esperado, resultado obtenido y estado final. Estos campos han sido seleccionado parecidos a los que se utilizaron en la asignatura de Validación y Pruebas de cuarto.

Estos casos se han seleccionado porque cubren las funcionalidades más críticas del sistema, especialmente aquellas relacionadas con la generación de reservas periódicas, la validación de restricciones, la carga de horarios desde hoja de cálculo y el análisis de la información extraída del fichero.

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-01 \\
		\textbf{Título} & Validación de restricciones con conflicto de aula y grupo \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema detecta correctamente los conflictos cuando se intenta mover una reserva periódica a una franja horaria en la que el aula está ocupada y el grupo ya tiene otra docencia asignada. \\
		\textbf{Precondición} & Debe existir una reserva periódica previa asociada a un aula, grupo, día y franja horaria concreta. \\
		\textbf{Postcondición} & La reserva periódica no se modifica, ya que la validación detecta conflictos. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear los datos de prueba necesarios: curso, semestre, día, grado, asignatura, grupo, aula y reserva periódica.
			\item Preparar una firma de serie válida para identificar la reserva.
			\item Simular mediante \textit{mocks} que la validación de ocupación de aula falla.
			\item Simular mediante \textit{mocks} que la validación de solapamiento de grupo falla.
			\item Ejecutar la función \textit{validar\_restricciones\_movimiento}.
			\item Comprobar que el resultado indica que la modificación no es válida.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Reserva periódica del grupo A1 en el aula A-01, lunes de 09:00 a 11:00. Movimiento simulado al martes de 10:00 a 12:00. \\
		\textbf{Resultado esperado} & El sistema debe devolver la validación como incorrecta e indicar los motivos del conflicto. \\
		\textbf{Resultado actual} & El sistema devuelve la validación como incorrecta e incluye los mensajes de aula ocupada y grupo solapado. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-01: validación de restricciones con conflicto}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-02 \\
		\textbf{Título} & Validación correcta de restricciones \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema permite la modificación de una reserva periódica cuando no existen conflictos de aula ni solapamientos de grupo. \\
		\textbf{Precondición} & Debe existir una reserva periódica previa y una firma válida que permita identificarla. \\
		\textbf{Postcondición} & La operación se considera válida y puede ejecutarse la modificación de la serie de reservas. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear una reserva periódica asociada a un grupo y aula.
			\item Preparar una firma válida de la serie de reservas.
			\item Simular mediante \textit{mocks} que el aula está disponible.
			\item Simular mediante \textit{mocks} que el grupo no tiene solapamientos.
			\item Ejecutar la función \textit{validar\_restricciones\_movimiento}.
			\item Comprobar que el resultado de la validación es correcto.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Movimiento de la reserva del grupo 101 al martes de 10:00 a 12:00 sin conflictos simulados. \\
		\textbf{Resultado esperado} & El sistema debe indicar que la modificación es válida y no devolver motivos de error. \\
		\textbf{Resultado actual} & El sistema devuelve la validación como correcta y la lista de motivos aparece vacía. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-02: validación correcta de restricciones}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-03 \\
		\textbf{Título} & Movimiento correcto de una serie de reservas periódicas \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema actualiza correctamente una serie de reservas periódicas cuando se modifica su día de la semana y su franja horaria. \\
		\textbf{Precondición} & Debe existir una reserva periódica registrada en la base de datos de prueba y una firma válida que permita identificar la serie. \\
		\textbf{Postcondición} & La reserva periódica queda actualizada con el nuevo día de la semana y la reserva asociada queda actualizada con la nueva franja horaria. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear una reserva periódica asociada a una reserva base.
			\item Preparar una firma válida para identificar la serie.
			\item Simular el cálculo de nuevas fechas mediante un \textit{mock}.
			\item Ejecutar la función \textit{mover\_serie\_reservas}.
			\item Refrescar los objetos desde la base de datos.
			\item Comprobar que el día de la semana se ha actualizado.
			\item Comprobar que la hora de inicio y la hora de fin se han actualizado correctamente.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Reserva original el lunes de 09:00 a 11:00. Nueva reserva el martes de 10:00 a 12:00. \\
		\textbf{Resultado esperado} & La reserva periódica debe cambiar al día 2 y la reserva asociada debe actualizar sus horas a 10:00 y 12:00. \\
		\textbf{Resultado actual} & La reserva periódica cambia correctamente al día 2 y la reserva asociada actualiza su hora de inicio y fin. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-03: movimiento correcto de una serie de reservas}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-04 \\
		\textbf{Título} & Generación correcta de reservas desde hoja de cálculo \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema genera correctamente reservas periódicas a partir de una clase extraída de una hoja de cálculo. \\
		\textbf{Precondición} & Deben existir en la base de datos de prueba el curso académico, semestre, día lectivo, aula, grado y asignatura correspondientes. \\
		\textbf{Postcondición} & Se crean las reservas periódicas necesarias, se crea el grupo si no existía previamente y el curso queda marcado como horario cargado. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear el calendario académico de prueba con un día lectivo, un festivo y un cambio de docencia.
			\item Crear el aula, grado y asignatura necesarios.
			\item Simular una clase leída del Excel mediante un objeto \textit{Clase}.
			\item Ejecutar la función \textit{generar\_reservas\_periodicas}.
			\item Comprobar que el curso queda marcado como cargado.
			\item Comprobar que se crea el grupo correspondiente.
			\item Comprobar que se crea una reserva en el día lectivo.
			\item Comprobar que se tiene en cuenta el cambio de docencia.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Clase de Álgebra, código 8797, grupo 1, aula Aula 51-A1, lunes de 09:00 a 11:00. \\
		\textbf{Resultado esperado} & El sistema debe crear el grupo, generar las reservas periódicas correspondientes y marcar el curso como horario cargado. \\
		\textbf{Resultado actual} & El sistema crea el grupo, genera la reserva del lunes, genera la reserva correspondiente al cambio de docencia y marca el curso como cargado. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-04: generación de reservas desde hoja de cálculo}
	\label{tab}
\end{table}


\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-05 \\
		\textbf{Título} & Carga de Excel con aula inexistente \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema no genera reservas cuando la clase extraída del Excel hace referencia a un aula que no existe en la base de datos. \\
		\textbf{Precondición} & Debe existir la asignatura correspondiente, pero no debe existir el aula indicada en la clase extraída del Excel. \\
		\textbf{Postcondición} & No se crea ninguna reserva en la base de datos de prueba. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear el curso, semestre, día lectivo, grado y asignatura necesarios.
			\item Crear una clase procedente del Excel con un aula inexistente.
			\item Ejecutar la función \textit{generar\_reservas\_periodicas}.
			\item Comprobar el número de reservas creadas.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Clase de Álgebra con aula ``Aula Inexistente''. \\
		\textbf{Resultado esperado} & El sistema no debe crear ninguna reserva. \\
		\textbf{Resultado actual} & El número de reservas creadas es 0. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-05: carga de Excel con aula inexistente}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-06 \\
		\textbf{Título} & Detección de aula ocupada \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema detecta correctamente que un aula ya está ocupada cuando se intenta crear o mover una reserva a una franja horaria solapada. \\
		\textbf{Precondición} & Debe existir una reserva periódica previa en el aula y franja horaria que se desea validar. \\
		\textbf{Postcondición} & La validación devuelve resultado negativo y no se permite utilizar el aula en esa franja. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear un aula y una reserva periódica asociada a ella.
			\item Ejecutar la función \textit{validar\_ocupacion\_aula} con una franja horaria solapada.
			\item Comprobar que la función devuelve \textit{False}.
			\item Comprobar que el mensaje de error indica que el aula está ocupada.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Aula 51-A1 ocupada de 09:00 a 11:00. Nueva validación de 10:00 a 12:00. \\
		\textbf{Resultado esperado} & El sistema debe indicar que el aula seleccionada ya está ocupada en ese horario. \\
		\textbf{Resultado actual} & El sistema devuelve la validación como incorrecta y muestra el mensaje de aula ocupada. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-06: detección de aula ocupada}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-07 \\
		\textbf{Título} & Validación de aula disponible por cambio de horario \\
		\textbf{Prioridad} & Media \\
		\textbf{Descripción} & Comprobar que el sistema considera disponible un aula cuando la nueva franja horaria no se solapa con reservas existentes. \\
		\textbf{Precondición} & Debe existir una reserva previa en el aula seleccionada, pero en una franja horaria diferente. \\
		\textbf{Postcondición} & La validación devuelve resultado positivo y el aula se considera disponible. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Crear una reserva periódica previa en un aula.
			\item Ejecutar la función \textit{validar\_ocupacion\_aula} con una franja horaria no solapada.
			\item Comprobar que la función devuelve \textit{True}.
			\item Comprobar que no se devuelve mensaje de error.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Aula 51-A1 ocupada de 09:00 a 11:00. Nueva validación de 11:00 a 13:00. \\
		\textbf{Resultado esperado} & El sistema debe considerar el aula disponible. \\
		\textbf{Resultado actual} & El sistema devuelve la validación como correcta y sin mensaje de error. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-07: validación de aula disponible}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-08 \\
		\textbf{Título} & Clasificación de celdas mediante expresiones regulares \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el analizador léxico clasifica correctamente las celdas procedentes de la hoja de cálculo según su contenido. \\
		\textbf{Precondición} & Debe estar disponible la función \textit{clasificar\_celda} utilizada por el analizador del Excel. \\
		\textbf{Postcondición} & Cada valor analizado se clasifica con el tipo correspondiente. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Ejecutar la función \textit{clasificar\_celda} con una celda vacía.
			\item Ejecutar la función con palabras clave como ``Hora'' o ``Nombre Abreviado''.
			\item Ejecutar la función con días de la semana.
			\item Ejecutar la función con tramos horarios.
			\item Ejecutar la función con grupos teóricos y prácticos.
			\item Ejecutar la función con aulas, códigos y abreviaturas de asignaturas.
			\item Comprobar que cada celda recibe la clasificación esperada.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Valores como ``Hora'', ``lunes'', ``09:00 - 11:00'', ``1'', ``801'', ``Aula 51-A1'', ``8797'' y ``ALG''. \\
		\textbf{Resultado esperado} & El sistema debe clasificar cada celda como comienzo de tabla, día de semana, tramo horario, grupo, aula, código o abreviatura según corresponda. \\
		\textbf{Resultado actual} & Las celdas se clasifican correctamente según las reglas definidas en el analizador léxico. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-08: clasificación de celdas mediante expresiones regulares}
	\label{tab}
\end{table}

\begin{table}[H]
	\centering
	\small
	\renewcommand{\arraystretch}{1.2}
	\begin{tabularx}{\textwidth}{@{}p{3.5cm}X@{}}
		\toprule
		\textbf{Campo} & \textbf{Descripción} \\
		\midrule
		\textbf{ID} & CP-09 \\
		\textbf{Título} & Normalización de nombres de aulas \\
		\textbf{Prioridad} & Alta \\
		\textbf{Descripción} & Comprobar que el sistema normaliza correctamente los nombres de aulas procedentes de la hoja de cálculo para poder compararlos con los nombres almacenados en la base de datos. \\
		\textbf{Precondición} & Debe estar disponible la función \textit{limpiar\_nombre\_aula}. \\
		\textbf{Postcondición} & Los nombres de aulas quedan transformados al formato esperado por la aplicación. \\
		\textbf{Pasos} &
		\begin{enumerate}
			\item Ejecutar la función con un valor vacío.
			\item Ejecutar la función con un aula con prefijo ``Aula''. \item Ejecutar la función con un nombre que no debe modificarse, como Aula Empresa''.
			\item Ejecutar la función con abreviaturas como ``Inf.''. \item Ejecutar la función con laboratorios abreviados como ``Lab.''.
			\item Comprobar que el resultado coincide con el formato normalizado esperado.
		\end{enumerate} \\
		\textbf{Datos de prueba} & Valores como \textit{None}, ``Aula 51-A1'',`` Aula Empresa'', ``Inf. 1'', ``Lab. Química'' y ``Lab 12-A1''. \\ \textbf{Resultado esperado} & El sistema debe devolver nombres normalizados como ``51-A1'', ``Aula Empresa'', ``Informática 1'' y ``Laboratorio Química''. \\\
		\textbf{Resultado actual} & Los nombres de aulas se normalizan correctamente según las reglas definidas. \\
		\textbf{Estado} & Correcto \\
		\bottomrule
	\end{tabularx}
	\caption{Caso de prueba CP-09: normalización de nombres de aulas}
	\label{tab}
\end{table}